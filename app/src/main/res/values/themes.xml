<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Leiriajeans" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/background_meio_bt</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Default application theme. -->
    <style name="Theme.Leiriajeans" parent="Base.Theme.Leiriajeans" />

    <!-- No Action Bar theme. -->
    <style name="Theme.Leiriajeans.NoActionBar" parent="Base.Theme.Leiriajeans">
        <item name="windowActionBar">false</item> <!-- This line disables the ActionBar -->
        <item name="windowNoTitle">true</item> <!-- This line hides the title bar -->
    </style>
</resources>
